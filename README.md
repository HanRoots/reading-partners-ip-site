# 阅读伙伴动物天团 IP 展示页

一个静态网页项目，用于展示 12 个阅读伙伴动物 IP 的故事、阅读能力路径和角色卡片。

## 内容

- `index.html`：页面结构和交互
- `lingokids-style.css`：Lingokids 风格视觉样式
- `assets/`：12 个角色及总览图素材

## 本地预览

直接打开 `index.html` 即可预览。

也可以运行：

```bash
python3 -m http.server 8796
```

然后访问 `http://127.0.0.1:8796/`。
